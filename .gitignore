# The ported original stays local.
# Without it the comparison in internal/actions/registry_test.go is skipped;
# the pinned count in TestRegistrySize still applies.
original

# Build artefacts
bin
dist
coverage.out

# Certificates, when generated locally
*.pem

# The fuzz corpus with found cases is kept, the cache is not
testdata/fuzz/*/[0-9a-f]*
